# CSS fallback fixed

The main stylesheet is now also embedded directly into `templates/base.html`, so Django pages keep the redesign even if static files are not served correctly during local development.
